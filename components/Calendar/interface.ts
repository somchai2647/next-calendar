export interface CallBackDate {
  day: number;
  month: number;
  year: number;
}

export interface calendarData {
  id: string;
  name: string;
  detail: string;
  allDay: boolean;
  startDate: number;
  endDate: number;
  bgColor: string;
}
